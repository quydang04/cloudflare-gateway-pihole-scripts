name: Update Filter Lists

on:
  schedule:
    # Run at 00:00 UTC+7 every 3 days (17:00 UTC của ngày trước đó)
    - cron: "0 17 */3 * *"
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_ENV: production
  SUMMARY_FILE: /tmp/filter_update_summary.txt

jobs:
  cgps:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          repository: "mrrfv/cloudflare-gateway-pihole-scripts"
          ref: "v1"
          
      - name: Prepare Summary File
        run: |
          VIETNAM_DATE=$(TZ="Asia/Ho_Chi_Minh" date "+%d/%m/%Y %H:%M:%S")
          echo "# Báo cáo cập nhật Cloudflare Gateway filter lists" > $SUMMARY_FILE
          echo "Thời gian bắt đầu: $VIETNAM_DATE (UTC+7)" >> $SUMMARY_FILE
          echo "----------------------------" >> $SUMMARY_FILE

      - name: Send Start Notification
        run: |
          VIETNAM_DATE=$(TZ="Asia/Ho_Chi_Minh" date "+%d/%m/%Y %H:%M:%S")
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d text="🔄 Bắt đầu cập nhật Cloudflare Gateway filter lists vào $VIETNAM_DATE (UTC+7)" \
            -d parse_mode=HTML

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".node-version"

      - name: Install npm dependencies
        run: |
          npm ci
          VIETNAM_DATE=$(TZ="Asia/Ho_Chi_Minh" date "+%d/%m/%Y %H:%M:%S")
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d text="✅ Đã cài đặt các dependencies vào $VIETNAM_DATE (UTC+7)" \
            -d parse_mode=HTML

      - name: Download allowlists
        id: download-allowlist
        run: |
          # Capture output for analysis
          OUTPUT=$(npm run download:allowlist 2>&1)
          echo "$OUTPUT"
          
          # Save the raw output for debugging
          echo "$OUTPUT" > /tmp/allowlist_output.log
          echo "Raw log output saved to /tmp/allowlist_output.log"
          
          # Đếm số lượng link trong biến ALLOWLIST_URLS
          if [ -n "$ALLOWLIST_URLS" ]; then
            # Đếm số dòng không trống trong biến ALLOWLIST_URLS
            FILTER_COUNT=$(echo "$ALLOWLIST_URLS" | grep -v "^[[:space:]]*$" | wc -l)
          else
            FILTER_COUNT="0"
          fi
          
          echo "Số lượng link allowlist: $FILTER_COUNT"
          
          # Add to summary
          echo "Allowlist domains: $FILTER_COUNT" >> $SUMMARY_FILE
          echo "ALLOWLIST_COUNT=$FILTER_COUNT" >> $GITHUB_ENV
          
          # Đếm số lượng domain trong file
          if [ -f "./allowlist.txt" ]; then
            DOMAIN_COUNT=$(wc -l < ./allowlist.txt | tr -d ' ')
          elif [ -f "./output/allowlist.txt" ]; then
            DOMAIN_COUNT=$(wc -l < ./output/allowlist.txt | tr -d ' ')
          elif [ -f "./dist/allowlist.txt" ]; then
            DOMAIN_COUNT=$(wc -l < ./dist/allowlist.txt | tr -d ' ')
          else
            # Tìm bất kỳ file nào có tên chứa "allowlist"
            ALLOWLIST_FILE=$(find . -name "*allowlist*" -type f | head -n 1)
            if [ -n "$ALLOWLIST_FILE" ]; then
              DOMAIN_COUNT=$(wc -l < "$ALLOWLIST_FILE" | tr -d ' ')
            else
              DOMAIN_COUNT="0"
            fi
          fi
          
          echo "Số domain trong allowlist: $DOMAIN_COUNT"
          echo "ALLOWLIST_DOMAINS=$DOMAIN_COUNT" >> $GITHUB_ENV
        env:
          ALLOWLIST_URLS: ${{ vars.ALLOWLIST_URLS }}

      - name: Notify Allowlists Downloaded
        run: |
          VIETNAM_DATE=$(TZ="Asia/Ho_Chi_Minh" date "+%d/%m/%Y %H:%M:%S")
          
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d text="📥 Đã tải xuống ${{ env.ALLOWLIST_COUNT }} filters trong allowlists vào $VIETNAM_DATE (UTC+7)" \
            -d parse_mode=HTML

      - name: Download blocklists
        id: download-blocklist
        run: |
          # Capture output for analysis
          OUTPUT=$(npm run download:blocklist 2>&1)
          echo "$OUTPUT"
          
          # Save the raw output for debugging
          echo "$OUTPUT" > /tmp/blocklist_output.log
          echo "Raw log output saved to /tmp/blocklist_output.log"
          
          # Đếm số lượng link trong biến BLOCKLIST_URLS
          if [ -n "$BLOCKLIST_URLS" ]; then
            # Đếm số dòng không trống trong biến BLOCKLIST_URLS
            FILTER_COUNT=$(echo "$BLOCKLIST_URLS" | grep -v "^[[:space:]]*$" | wc -l)
          else
            FILTER_COUNT="0"
          fi
          
          echo "Số lượng link blocklist: $FILTER_COUNT"
          
          # Add to summary
          echo "Blocklist domains: $FILTER_COUNT" >> $SUMMARY_FILE
          echo "BLOCKLIST_COUNT=$FILTER_COUNT" >> $GITHUB_ENV
          
          # Đếm số lượng domain trong file
          if [ -f "./blocklist.txt" ]; then
            DOMAIN_COUNT=$(wc -l < ./blocklist.txt | tr -d ' ')
          elif [ -f "./output/blocklist.txt" ]; then
            DOMAIN_COUNT=$(wc -l < ./output/blocklist.txt | tr -d ' ')
          elif [ -f "./dist/blocklist.txt" ]; then
            DOMAIN_COUNT=$(wc -l < ./dist/blocklist.txt | tr -d ' ')
          else
            # Tìm bất kỳ file nào có tên chứa "blocklist"
            BLOCKLIST_FILE=$(find . -name "*blocklist*" -type f | head -n 1)
            if [ -n "$BLOCKLIST_FILE" ]; then
              DOMAIN_COUNT=$(wc -l < "$BLOCKLIST_FILE" | tr -d ' ')
            else
              DOMAIN_COUNT="0"
            fi
          fi
          
          echo "Số domain trong blocklist: $DOMAIN_COUNT"
          echo "BLOCKLIST_DOMAINS=$DOMAIN_COUNT" >> $GITHUB_ENV
          
          # Tính tổng số domains
          TOTAL_DOMAINS=$((DOMAIN_COUNT + ${ALLOWLIST_DOMAINS:-0}))
          echo "TOTAL_DOMAINS=$TOTAL_DOMAINS" >> $GITHUB_ENV
        env:
          BLOCKLIST_URLS: ${{ vars.BLOCKLIST_URLS }}

      - name: Notify Blocklists Downloaded
        run: |
          VIETNAM_DATE=$(TZ="Asia/Ho_Chi_Minh" date "+%d/%m/%Y %H:%M:%S")
          
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d text="📥 Đã tải xuống ${{ env.BLOCKLIST_COUNT }} filters trong blocklists vào $VIETNAM_DATE (UTC+7)" \
            -d parse_mode=HTML

      - name: Debug Directory Structure
        run: |
          echo "=== DEBUG: Hiển thị cấu trúc thư mục ==="
          find . -type f -name "*list*" | sort
          echo "=== DEBUG: Kết thúc hiển thị ==="

      - name: Debug Log Values
        run: |
          echo "=== DEBUG: Các giá trị đã tính ==="
          echo "ALLOWLIST_COUNT: ${{ env.ALLOWLIST_COUNT }}"
          echo "BLOCKLIST_COUNT: ${{ env.BLOCKLIST_COUNT }}"
          echo "ALLOWLIST_DOMAINS: ${{ env.ALLOWLIST_DOMAINS }}"
          echo "BLOCKLIST_DOMAINS: ${{ env.BLOCKLIST_DOMAINS }}"
          echo "TOTAL_DOMAINS: ${{ env.TOTAL_DOMAINS }}"
          echo "=== DEBUG: Kết thúc hiển thị ==="

      - name: Delete old rules and lists
        id: delete-rules
        run: |
          # Capture output for analysis
          OUTPUT=$(npm run cloudflare-delete 2>&1)
          echo "$OUTPUT"
          
          # Lưu output ra file để debug
          echo "$OUTPUT" > /tmp/delete_output.log
          
          # Tìm kiếm trong log với các pattern phổ biến
          DELETED_LISTS=0
          DELETED_RULES=0
          
          # Pattern cho lists
          if grep -q -i "deleted.*list" /tmp/delete_output.log; then
            DELETED_LISTS=$(grep -i "deleted.*list" /tmp/delete_output.log | grep -o -E '[0-9]+' | head -1)
          elif grep -q -i "removed.*list" /tmp/delete_output.log; then
            DELETED_LISTS=$(grep -i "removed.*list" /tmp/delete_output.log | grep -o -E '[0-9]+' | head -1)
          fi
          
          # Pattern cho rules
          if grep -q -i "deleted.*rule" /tmp/delete_output.log; then
            DELETED_RULES=$(grep -i "deleted.*rule" /tmp/delete_output.log | grep -o -E '[0-9]+' | head -1)
          elif grep -q -i "removed.*rule" /tmp/delete_output.log; then
            DELETED_RULES=$(grep -i "removed.*rule" /tmp/delete_output.log | grep -o -E '[0-9]+' | head -1)
          fi
          
          # Đảm bảo có giá trị mặc định
          if [ -z "$DELETED_LISTS" ]; then DELETED_LISTS="0"; fi
          if [ -z "$DELETED_RULES" ]; then DELETED_RULES="0"; fi
          
          echo "DEBUG: DELETED_LISTS=$DELETED_LISTS, DELETED_RULES=$DELETED_RULES"
          
          # Add to summary and environment variables
          echo "DELETED_LISTS=$DELETED_LISTS" >> $GITHUB_ENV
          echo "DELETED_RULES=$DELETED_RULES" >> $GITHUB_ENV
          echo "Deleted lists: $DELETED_LISTS" >> $SUMMARY_FILE
          echo "Deleted rules: $DELETED_RULES" >> $SUMMARY_FILE
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_LIST_ITEM_LIMIT: ${{ secrets.CLOUDFLARE_LIST_ITEM_LIMIT }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

      - name: Notify Delete Completed
        run: |
          VIETNAM_DATE=$(TZ="Asia/Ho_Chi_Minh" date "+%d/%m/%Y %H:%M:%S")
          
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d text="🗑️ Đã xóa ${{ env.DELETED_LISTS }} lists và ${{ env.DELETED_RULES }} rules vào $VIETNAM_DATE (UTC+7)" \
            -d parse_mode=HTML

      - name: Create new rules and lists
        id: create-rules
        run: |
          # Thực hiện lệnh tạo
          OUTPUT=$(npm run cloudflare-create 2>&1)
          echo "$OUTPUT"
          
          # Lưu output ra file để debug
          echo "$OUTPUT" > /tmp/create_output.log
          
          # Tìm kiếm trong log với các pattern phổ biến
          CREATED_LISTS=0
          CREATED_RULES=0
          
          # Pattern cho lists
          if grep -q -i "created.*list" /tmp/create_output.log; then
            CREATED_LISTS=$(grep -i "created.*list" /tmp/create_output.log | grep -o -E '[0-9]+' | head -1)
          elif grep -q -i "new.*list" /tmp/create_output.log; then
            CREATED_LISTS=$(grep -i "new.*list" /tmp/create_output.log | grep -o -E '[0-9]+' | head -1)
          fi
          
          # Pattern cho rules
          if grep -q -i "created.*rule" /tmp/create_output.log; then
            CREATED_RULES=$(grep -i "created.*rule" /tmp/create_output.log | grep -o -E '[0-9]+' | head -1)
          elif grep -q -i "new.*rule" /tmp/create_output.log; then
            CREATED_RULES=$(grep -i "new.*rule" /tmp/create_output.log | grep -o -E '[0-9]+' | head -1)
          fi
          
          # Đảm bảo có giá trị mặc định
          if [ -z "$CREATED_LISTS" ]; then CREATED_LISTS="0"; fi
          if [ -z "$CREATED_RULES" ]; then CREATED_RULES="0"; fi
          
          # Sử dụng tổng số domain đã đếm trước đó
          ADDED_DOMAINS="${{ env.TOTAL_DOMAINS }}"
          if [ -z "$ADDED_DOMAINS" ]; then
            ADDED_DOMAINS="0"
          fi
          
          echo "DEBUG: CREATED_LISTS=$CREATED_LISTS, CREATED_RULES=$CREATED_RULES, ADDED_DOMAINS=$ADDED_DOMAINS"
          
          # Add to summary and environment variables
          echo "CREATED_LISTS=$CREATED_LISTS" >> $GITHUB_ENV
          echo "CREATED_RULES=$CREATED_RULES" >> $GITHUB_ENV
          echo "ADDED_DOMAINS=$ADDED_DOMAINS" >> $GITHUB_ENV
          echo "Created lists: $CREATED_LISTS" >> $SUMMARY_FILE
          echo "Created rules: $CREATED_RULES" >> $SUMMARY_FILE
          echo "Added domains: $ADDED_DOMAINS" >> $SUMMARY_FILE
        env:
          BLOCK_PAGE_ENABLED: ${{ vars.BLOCK_PAGE_ENABLED }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_LIST_ITEM_LIMIT: ${{ secrets.CLOUDFLARE_LIST_ITEM_LIMIT }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}

      - name: Notify Create Completed
        run: |
          VIETNAM_DATE=$(TZ="Asia/Ho_Chi_Minh" date "+%d/%m/%Y %H:%M:%S")
          
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d text="🆕 Đã tạo ${{ env.CREATED_LISTS }} lists, ${{ env.CREATED_RULES }} rules và thêm ${{ env.ADDED_DOMAINS }} domains vào $VIETNAM_DATE (UTC+7)" \
            -d parse_mode=HTML

      - name: Generate Final Summary
        if: always()
        run: |
          VIETNAM_DATE=$(TZ="Asia/Ho_Chi_Minh" date "+%d/%m/%Y %H:%M:%S")
          echo "----------------------------" >> $SUMMARY_FILE
          echo "Thời gian hoàn thành: $VIETNAM_DATE (UTC+7)" >> $SUMMARY_FILE
          echo "Trạng thái: ${{ job.status }}" >> $SUMMARY_FILE
          
          # Format summary for Telegram
          SUMMARY=$(cat $SUMMARY_FILE)
          
          if [[ "${{ job.status }}" == "success" ]]; then
            STATUS_EMOJI="✅"
            STATUS_TEXT="hoàn thành"
          else
            STATUS_EMOJI="⚠️"
            STATUS_TEXT="thất bại"
          fi
          
          # Fix: Simplify message creation by using single quotes and avoiding backticks in YAML
          echo "${STATUS_EMOJI} Quá trình cập nhật Cloudflare Gateway filter lists đã ${STATUS_TEXT}" > /tmp/final_message.txt
          echo "" >> /tmp/final_message.txt  # Empty line
          echo "📊 *Báo cáo tóm tắt:*" >> /tmp/final_message.txt
          echo '```' >> /tmp/final_message.txt  # Use single quotes to avoid YAML issues
          cat $SUMMARY_FILE >> /tmp/final_message.txt
          echo '```' >> /tmp/final_message.txt  # Use single quotes to avoid YAML issues

      - name: Send Final Summary
        if: always()
        run: |
          # Send the full report using Markdown format
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TELEGRAM_CHAT_ID }} \
            -d text="$(cat /tmp/final_message.txt)" \
            -d parse_mode=Markdown

      - name: Send ping request
        if: env.PING_URL != ''
        run: |
          curl "${{ env.PING_URL }}"
        env:
          PING_URL: ${{ secrets.PING_URL }}

  keepalive:
    if: github.event_name == 'schedule'
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - uses: liskin/gh-workflow-keepalive@v1
